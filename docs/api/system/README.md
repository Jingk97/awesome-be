# 模块：system
## 默认规则
| 项目     | 规范                              |
| -------- | --------------------------------- |
| 基础路径 | `/api/v1`                         |
| 数据格式 | `application/json`                |
| 字符编码 | `UTF-8`                           |
| 时间格式 | Unix 毫秒时间戳（数字类型）       |
| 布尔值   | `true` / `false`（JSON 原生布尔） |
## 统一响应结构
所有接口均返回以下结构：

```JSON
{
  "code": 0,
  "message": "ok",
  "data": {},
  "request_id": "request-id"
}
```

| 字段         | 类型        | 说明                      |
| ------------ | ----------- | ------------------------- |
| `code`       | integer     | 业务状态码，0 表示成功    |
| `message`    | string      | 状态描述，成功时为 "ok"   |
| `data`       | object/null | 业务数据，失败时可为 null |
| `request_id` | string      | 请求唯一 ID，用于日志追踪 |
### 模块错误码定义
  | 错误码范围 | HTTP 状态码 | 说明       |
  | ---------- | ----------- | ---------- |
  | 0          | 200         | 成功       |
  | 1xxx       | 500         | 系统错误   |
  | 401x       | 401         | 认证错误   |
  | 404x       | 404         | 资源不存在 |
  | 429x       | 429         | 限流错误   |
  | 5xxx       | 500         | 服务器错误 |
## 接口索引

列出本模块下的接口文档文件，并附简短描述。

- [/api/v1/system/config](./config.md)：通用系统配置，用于返回系统版本、服务器时间、配置的默认登录方式等
- 

